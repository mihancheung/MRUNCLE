<view class="custom_nav {{ !isShowBack ? 'custom_nav_noback' : ''}}" style="padding-top: {{statusBarHeigth}}px">
  <view class="btns_box">
    <view class="icon_box" bindtap="back" wx:if="{{isShowBack}}">
      <image mode="heightFix" class="back_icon" src="./images/back.svg" />
    </view>
    <view class="line" wx:if="{{isShowBack}}"></view>
    <view class="icon_box" bindtap="backHome">
      <image mode="heightFix" class="home_icon" src="./images/home.svg" />
    </view>
  </view>
</view>